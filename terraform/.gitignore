# Local state and any secrets. Never commit these.
*.tfstate
*.tfstate.*
.terraform/
terraform.tfvars
*.pem
crash.log

# Saved plans embed the variable values they were created with, including the
# RDS master password, so they are as sensitive as state.
tfplan
*.tfplan
plan.json
