import type { LucideIcon } from 'lucide-react'

export interface AppIconProps {
  Icon: LucideIcon
  size?: number
  color?: string
  fill?: string
  strokeWidth?: number
  style?: React.CSSProperties
}
