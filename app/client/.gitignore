node_modules
dist
out
.DS_Store
.eslintcache
*.log
*.log.*
.env.*
!.env.example

# electron-vite writes a timestamped compiled config next to the source when it
# can't load the .ts directly; it's a transient build artifact.
electron.vite.config.*.mjs